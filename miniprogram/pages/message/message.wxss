/* pages/message/message.wxss */
.container {
  background: #fff;
  padding: 25rpx;
  height: 100vh; /* 占满整个屏幕高度 */
  display: flex;
  flex-direction: column;
}

/* 顶部导航栏样式 */
.nav-bar {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 80rpx;
  background: #f8f8f8;
  border-bottom: 2rpx solid #eee;
}
.nav-left {
  position: absolute;
  left: 20rpx;
}
.back-icon { width: 30rpx; }
.nav-title {
  font-size: 36rpx;
  font-weight: bold;
}

/* 无消息提示区域样式 */
.no-message-container {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}
.no-message-img {
  width: 400rpx;
  margin-bottom: 20rpx;
}
.no-message-tip {
  font-size: 30rpx;
  color: #999;
}